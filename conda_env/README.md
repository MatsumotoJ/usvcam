# Conda environment files for USVCAM

- `usvcam.yaml`: Base environment file.
- `usvcam_win11_202512.yaml`: Version-pinned environment that was confirmed to work on Windows 11 (December 2025), exported using `conda env export --from-history`.